# 当前学习回合

入口 SKILL.md 是实时流程的唯一规则。先看当前原句和原样作答，区分词义、搭配、主干、从句、范围与中文自然度；然后一次 query-personal-summary 按点哈希查询。命中必读，未命中即止。历史只能改变解释方式，不能代替当前诊断。

已完整给出的句子不重复检索；普通轮不读 Capture 合同、frontier、源码、全库或 T9，不封包。10–20 秒目标计入完整学习正文，不能用首字达标。每轮零到一个诊断问题；需要直接讲解时不延长猜测。

在上下文内保留当前单元的起始 turn、后续 turn、原图定位和已展示内容，不为记录这些信息每轮写文件。用户要求保存时导出实际对话并按完整单元保存合同执行。捕获失败只约束下一次写入，不阻止正常讲解。
