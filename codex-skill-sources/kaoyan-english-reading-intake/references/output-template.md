# Reading Intake Output Contract

Use plain-text labels. Report the saved source/locator, complete dialogue and
attachment coverage, actionable missing fields, and whether the segment is ready.
Keep package/token hashes and full verification fields in the durable receipt;
show them only when requested or needed to explain a failure.

Do not repeat protected answers to prove storage. Never claim capture or formal
intake without its actual matching receipt. Raw capture has formal_write_count=0
and background_processing=none; successful web-route teaching captures provide a real per-segment ZIP link without internal hashes.
